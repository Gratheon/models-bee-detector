# YOLOv5 requirements - CPU-only runtime (production inference)
# Keep this file focused on what server.py/detect.py need at runtime.

numpy>=1.23.0
opencv-python-headless>=4.8.0
Pillow>=9.0.0
psutil>=5.9.0
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.65.0
gratheon-log-lib @ https://github.com/Gratheon/log-lib-py/archive/03b30ba.zip

# Deterministic CPU-only PyTorch wheels.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch==2.10.0+cpu
torchvision==0.25.0+cpu
